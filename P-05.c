#include<stdio.h>
int main()
{ // WAP to print the multiplication table of given number n ;
		int n,i;
		printf("Enter the value of n =",n);
		scanf("%d",&n);
		
		for( n;i<n;i++)
		{
		
			printf("Multiplication of given n =  %d %d\n",i*n);
		}
return 0;
}
