#include<stdio.h>
int main()
{
			int length,breadth;
			
			printf("Given length:",length);
			scanf("%d",&length);
			
			printf("Given breadth:",breadth);
			scanf("%d",&breadth);
			
			int area=length*breadth;
			printf("Total area of rectangle: %d",area);
			
return 0;
}
