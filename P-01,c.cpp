#include <stdio.h>
int main()
{ // finding first n natural numbers using for loop
			int a,i;
			printf("Enter the number",a);
			scanf("%d",&a);
			for(a; i<a; i++)
			{
				printf("Output = %d\n",i+1);
			}
return 0;
}
