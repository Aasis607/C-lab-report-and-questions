#include<stdio.h>
int main()
{ // let a be assumed as user input or value
// Here we have to find the first n natural number using do while loop;
				int a,i=0;
				
					printf("Enter the input ",a);
					scanf("%d",&a);
					
			 	do
			 	{
			 		printf("Output = %d \n",i);
			 	i++;
				 }
				
				while(i<=a);
			
				
	return 0;
}
